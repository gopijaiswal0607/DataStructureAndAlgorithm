#include<iostream>
#include<string>
using namespace std;
int convert_stoi(string s,auto it)
{
if(it==s.end())
      return result;
else
    return *it
}
int main()
{
string s;
cin>>s;
auto it=s.begin();
cout<<convert_stoi(s,it);
return 0;
}
